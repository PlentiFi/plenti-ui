import React, { useState, useEffect } from "react";

import ComingSoon from 'components/ComingSoon'

type VAULTS_TAB_TYPE = "add" | "remove";

export default function Porfolio({ library, state, dispatch, connectWallet }) {
   return (
     <ComingSoon />
   )
}
